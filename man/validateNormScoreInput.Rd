% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score-components.R
\name{validateNormScoreInput}
\alias{validateNormScoreInput}
\title{Validate and prepare inputs for normScore}
\usage{
validateNormScoreInput(
  normalizedDataList,
  groupData,
  rawData,
  refGroup,
  altGroup,
  returnDetails,
  nBoot
)
}
\arguments{
\item{normalizedDataList}{A named list of normalized data matrices or data
frames. Each element should contain proteins in rows and samples in columns.}

\item{groupData}{A data frame or object coercible to a data frame containing
sample-group annotation. The first column is assumed to contain sample names
and the second column group labels.}

\item{rawData}{A matrix or data frame containing raw intensity values, with
proteins in rows and samples in columns.}

\item{refGroup}{Character string indicating the reference group used for
group-comparison metrics. If \code{NULL} or invalid, it is automatically set.}

\item{altGroup}{Character string indicating the alternative group used for
group-comparison metrics. If \code{NULL} or invalid, it is automatically set.}

\item{returnDetails}{Logical value indicating whether detailed results should
be returned by \code{normScore()}.}

\item{nBoot}{Numeric value indicating the number of bootstrap resamples.}
}
\value{
A list containing validated and prepared inputs: \code{normalizedDataList},
\code{groupData}, \code{rawData}, \code{refGroup}, \code{altGroup}, \code{returnDetails}, \code{nBoot},
and \code{singleGroup}.
}
\description{
Checks the input objects used by \code{normScore()}, including group annotation,
raw data, normalized datasets, sample names, protein names, and bootstrap
settings. The function also aligns the order of rows and columns across
normalized datasets, adds the log-transformed dataset when required, and
identifies whether the analysis contains a single group.
}
\keyword{internal}
