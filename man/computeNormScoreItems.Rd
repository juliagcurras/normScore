% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score-components.R
\name{computeNormScoreItems}
\alias{computeNormScoreItems}
\title{Compute normScore item scores}
\usage{
computeNormScoreItems(
  normalizedDataList,
  groupData,
  rawData,
  refGroup,
  altGroup,
  singleGroup
)
}
\arguments{
\item{normalizedDataList}{A named list of normalized data matrices, with
proteins in rows and samples in columns.}

\item{groupData}{A data frame containing sample-group annotation, with sample
names in the first column and group labels in the second column.}

\item{rawData}{A matrix or data frame containing raw intensity values. Used
for consistency with the main workflow.}

\item{refGroup}{Character string indicating the reference group used for the
MA-plot item.}

\item{altGroup}{Character string indicating the alternative group used for the
MA-plot item.}

\item{singleGroup}{Logical value indicating whether the input data contain a
single group.}
}
\value{
A data frame with normalization methods in rows and normScore items
in columns. If \code{singleGroup = TRUE}, the MA-plot item is omitted.
}
\description{
Computes the individual quality items used by \code{normScore()} for each
normalization method. These include within-group variability, sample
correlation, MA-plot deviation, mean-SD trend, RLE deviation, and total
intensity consistency. When only one group is available, the MA-plot item is
skipped.
}
\keyword{internal}
