Met2 = (data*median(data, na.rm = T))/mean(data, na.rm = T),
Met3 = (data/median(data, na.rm = T))*mean(data, na.rm = T)
)
resNS <- sapply(lista, normScore::withinGroupCorrelations, groupData = simData$metadata,
method = "spearman", simplify = T, USE.NAMES = T)
View(resNS)
simData <- simulateData(nProteins = 100, seed = 9396)
data <- simData$logData
lista <- list(
Log = data,
Met1 = data+0.5,
Met2 = (data*median(data, na.rm = T))/mean(data, na.rm = T),
Met3 = (data/median(data, na.rm = T))*mean(data, na.rm = T)
)
res <- normScore::withinGroupCorrelations(
data = lista[["Log"]],
groupData = simData$metadata,
method = "spearman")
resNS <- sapply(lista, normScore::withinGroupCorrelations, groupData = simData$metadata,
method = "spearman", simplify = T, USE.NAMES = T)
Biomics::plotBoxMulti(resNS, varResumen = colnames(resNS), interact = F)
# same result in normScore than in NormalyzerDE???
library(dplyr)
Biomics::plotBoxMulti(resNS, varResumen = colnames(resNS), interact = F)
library(ggplot2)
# Just checking item 2: correlation
# same result in normScore than in NormalyzerDE???
library(dplyr)
library(ggplot2)
#............................................................................####
# normScore ####
library(normScore)
simData <- simulateData(nProteins = 100, seed = 9396)
data <- simData$logData
lista <- list(
Log = data,
Met1 = data+0.5,
Met2 = (data*median(data, na.rm = T))/mean(data, na.rm = T),
Met3 = (data/median(data, na.rm = T))*mean(data, na.rm = T)
)
resNS <- sapply(lista, normScore::withinGroupCorrelations, groupData = simData$metadata,
method = "spearman", simplify = T, USE.NAMES = T)
Biomics::plotBoxMulti(resNS, varResumen = colnames(resNS), interact = F)
seq(1,40, 2)
lista <- list(
Log = data,
Met1 = data+0.5,
Met2 = (data*median(data, na.rm = T))/mean(data, na.rm = T),
Met3 = data[,seq(1,40, 2)] + rnorm(n = 20, mean = 0.3, sd = 5)
)
# Just checking item 2: correlation
# same result in normScore than in NormalyzerDE???
library(dplyr)
library(ggplot2)
#............................................................................####
# normScore ####
library(normScore)
simData <- simulateData(nProteins = 100, seed = 9396)
data <- simData$logData
lista <- list(
Log = data,
Met1 = data+0.5,
Met2 = (data*median(data, na.rm = T))/mean(data, na.rm = T),
Met3 = data[,seq(1,40, 2)] + rnorm(n = 20, mean = 0.3, sd = 5)
)
resNS <- sapply(lista, normScore::withinGroupCorrelations, groupData = simData$metadata,
method = "spearman", simplify = T, USE.NAMES = T)
View(lista)
View(lista$Met3)
seq(2,40, 2)
# Just checking item 2: correlation
# same result in normScore than in NormalyzerDE???
library(dplyr)
library(ggplot2)
#............................................................................####
# normScore ####
library(normScore)
simData <- simulateData(nProteins = 100, seed = 9396)
data <- simData$logData
lista <- list(
Log = data,
Met1 = data+0.5,
Met2 = (data*median(data, na.rm = T))/mean(data, na.rm = T),
Met3 = cbind(data[,seq(1,40, 2)] + rnorm(n = 20, mean = 0.3, sd = 5), data[,seq(2,40, 2)])
)
resNS <- sapply(lista, normScore::withinGroupCorrelations, groupData = simData$metadata,
method = "spearman", simplify = T, USE.NAMES = T)
Biomics::plotBoxMulti(resNS, varResumen = colnames(resNS), interact = F)
#............................................................................####
# NormalyzerDE ####
calculateCorrSum <- function(methodData, allReplicateGroups,
sampleGroupsWithReplicates, corrType) {
corSums <- vector()
for (groupNbr in seq_along(sampleGroupsWithReplicates)) {
specificReplicateVals <- as.matrix(
methodData[, which(allReplicateGroups == sampleGroupsWithReplicates[groupNbr])])
class(specificReplicateVals) <- "numeric"
corVals <- stats::cor(
specificReplicateVals ,
use="pairwise.complete.obs",
method=corrType)
for (index in seq_len(ncol(specificReplicateVals) - 1)) {
corSums <- c(
corSums,
corVals[index, -(seq_len(index)), drop="FALSE"]
)
}
}
corSums
}
calculateSummarizedCorrelationVector <- function(
methodlist, allReplicateGroups, sampleGroupsWithReplicates, corrType) {
validCorrTypes <- c("pearson", "spearman")
if (!corrType %in% validCorrTypes) {
stop("Unknown correlation type: ",
corrType,
" valid are: ",
paste(validCorrTypes, collapse=", "))
}
corr_combination_count <- function(allReplicateGroups) {
replicate_counts <- table(allReplicateGroups)
sum(vapply(
replicate_counts,
function(count) { (count * (count-1)) / 2 },
0))
}
avgCorSum <- vapply(
methodlist,
calculateCorrSum,
rep(0, corr_combination_count(allReplicateGroups)),
allReplicateGroups=allReplicateGroups,
sampleGroupsWithReplicates=sampleGroupsWithReplicates,
corrType=corrType
)
avgCorSum
}
resNDE <- calculateSummarizedCorrelationVector(
methodlist = lista,
sampleGroupsWithReplicates = unique(simData$metadata$Groups),
allReplicateGroups = simData$metadata$Groups,
corrType = "spearman"
)
Biomics::plotBoxMulti(resNDE, varResumen = colnames(resNS), interact = F)
View(resNDE)
View(resNS)
summary(arsenal::comparedf(x = resNS, y = resNDE, by = 0))
resNDE <- as.data.frame(resNDE)
resNDE <- as.data.frame(resNDE)
summary(arsenal::comparedf(x = resNS, y = resNDE, by = 0))
resNDE <- as.data.frame(resNDE)
resNDE <- as.data.frame(resNDE)
arsenal::comparedf(x = resNS, y = resNDE, by = 0)
is.data.frame(resNS)
# Just checking item 2: correlation
# same result in normScore than in NormalyzerDE???
library(dplyr)
library(ggplot2)
#............................................................................####
# normScore ####
library(normScore)
simData <- simulateData(nProteins = 100, seed = 9396)
data <- simData$logData
lista <- list(
Log = data,
Met1 = data+0.5,
Met2 = (data*median(data, na.rm = T))/mean(data, na.rm = T),
Met3 = cbind(data[,seq(1,40, 2)] + rnorm(n = 20, mean = 0.3, sd = 5), data[,seq(2,40, 2)])
)
resNS <- sapply(lista, normScore::withinGroupCorrelations, groupData = simData$metadata,
method = "spearman", simplify = T, USE.NAMES = T)
resNS <- as.data.frame(resNS)
Biomics::plotBoxMulti(resNS, varResumen = colnames(resNS), interact = F)
is.data.frame(resNS)
calculateCorrSum <- function(methodData, allReplicateGroups,
sampleGroupsWithReplicates, corrType) {
corSums <- vector()
for (groupNbr in seq_along(sampleGroupsWithReplicates)) {
specificReplicateVals <- as.matrix(
methodData[, which(allReplicateGroups == sampleGroupsWithReplicates[groupNbr])])
class(specificReplicateVals) <- "numeric"
corVals <- stats::cor(
specificReplicateVals ,
use="pairwise.complete.obs",
method=corrType)
for (index in seq_len(ncol(specificReplicateVals) - 1)) {
corSums <- c(
corSums,
corVals[index, -(seq_len(index)), drop="FALSE"]
)
}
}
corSums
}
calculateSummarizedCorrelationVector <- function(
methodlist, allReplicateGroups, sampleGroupsWithReplicates, corrType) {
validCorrTypes <- c("pearson", "spearman")
if (!corrType %in% validCorrTypes) {
stop("Unknown correlation type: ",
corrType,
" valid are: ",
paste(validCorrTypes, collapse=", "))
}
corr_combination_count <- function(allReplicateGroups) {
replicate_counts <- table(allReplicateGroups)
sum(vapply(
replicate_counts,
function(count) { (count * (count-1)) / 2 },
0))
}
avgCorSum <- vapply(
methodlist,
calculateCorrSum,
rep(0, corr_combination_count(allReplicateGroups)),
allReplicateGroups=allReplicateGroups,
sampleGroupsWithReplicates=sampleGroupsWithReplicates,
corrType=corrType
)
avgCorSum
}
resNDE <- calculateSummarizedCorrelationVector(
methodlist = lista,
sampleGroupsWithReplicates = unique(simData$metadata$Groups),
allReplicateGroups = simData$metadata$Groups,
corrType = "spearman"
)
resNDE <- as.data.frame(resNDE)
is.data.frame(resNDE)
Biomics::plotBoxMulti(resNDE, varResumen = colnames(resNS), interact = F)
summary(arsenal::comparedf(x = resNS, y = resNDE, by = 0))
summary(arsenal::comparedf(x = resNS, y = resNDE, by = "row.names"))
View(resNDE)
View(resNS)
withinGroupCorrelations <- function(data, groupData, method = "spearman") {
data <- as.data.frame(data)
groupData <- as.data.frame(groupData)
allCorrelations <- lapply(unique(groupData$Groups), function(group) {
samplesByGroup <- groupData[groupData$Groups == group, "Samples"]
groupMatrix <- data[, samplesByGroup, drop = FALSE]
corMatrix <- stats::cor(groupMatrix, use = "pairwise.complete.obs", method = method)
corMatrix[lower.tri(corMatrix)] <- NA
diag(corMatrix) <- NA
correlationValues <- as.vector(corMatrix)
correlationValues <- stats::na.omit(correlationValues)
return(correlationValues)
})
correlationVector <- unlist(allCorrelations, use.names = FALSE)
return(correlationVector)
}
resNS <- sapply(lista, withinGroupCorrelations, groupData = simData$metadata,
method = "spearman", simplify = T, USE.NAMES = T)
resNS <- as.data.frame(resNS)
is.data.frame(resNS)
Biomics::plotBoxMulti(resNS, varResumen = colnames(resNS), interact = F)
withinGroupCorrelations <- function(data, groupData, method = "spearman") {
data <- as.data.frame(data)
groupData <- as.data.frame(groupData)
allCorrelations <- lapply(unique(groupData$Groups), function(group) {
samplesByGroup <- groupData[groupData$Groups == group, "Samples"]
groupMatrix <- data[, samplesByGroup, drop = FALSE]
corMatrix <- stats::cor(groupMatrix, use = "pairwise.complete.obs", method = method)
corValues <- c()
for (i in seq_len(ncol(groupMatrix) - 1)) {
corValues <- c(corValues, corMatrix[i, -(seq_len(i))])
}
corValues
})
unlist(allCorrelations, use.names = FALSE)
return(correlationVector)
}
resNS <- sapply(lista, withinGroupCorrelations, groupData = simData$metadata,
method = "spearman", simplify = T, USE.NAMES = T)
resNS <- as.data.frame(resNS)
withinGroupCorrelations <- function(data, groupData, method = "spearman") {
data <- as.data.frame(data)
groupData <- as.data.frame(groupData)
allCorrelations <- lapply(unique(groupData$Groups), function(group) {
samplesByGroup <- groupData[groupData$Groups == group, "Samples"]
groupMatrix <- data[, samplesByGroup, drop = FALSE]
corMatrix <- stats::cor(groupMatrix, use = "pairwise.complete.obs", method = method)
corValues <- c()
for (i in seq_len(ncol(groupMatrix) - 1)) {
corValues <- c(corValues, corMatrix[i, -(seq_len(i))])
}
corValues
})
unlist(allCorrelations, use.names = FALSE)
return(correlationVector)
}
resNS <- sapply(lista, withinGroupCorrelations, groupData = simData$metadata,
method = "spearman", simplify = T, USE.NAMES = T)
withinGroupCorrelations <- function(data, groupData, method = "spearman") {
data <- as.data.frame(data)
groupData <- as.data.frame(groupData)
allCorrelations <- lapply(unique(groupData$Groups), function(group) {
samplesByGroup <- groupData[groupData$Groups == group, "Samples"]
groupMatrix <- data[, samplesByGroup, drop = FALSE]
corMatrix <- stats::cor(groupMatrix, use = "pairwise.complete.obs", method = method)
corValues <- c()
for (i in seq_len(ncol(groupMatrix) - 1)) {
corValues <- c(corValues, corMatrix[i, -(seq_len(i))])
}
corValues
})
unlist(allCorrelations, use.names = FALSE)
}
resNS <- sapply(lista, withinGroupCorrelations, groupData = simData$metadata,
method = "spearman", simplify = T, USE.NAMES = T)
resNS <- as.data.frame(resNS)
is.data.frame(resNS)
Biomics::plotBoxMulti(resNS, varResumen = colnames(resNS), interact = F)
#............................................................................####
# NormalyzerDE ####
calculateCorrSum <- function(methodData, allReplicateGroups,
sampleGroupsWithReplicates, corrType) {
corSums <- vector()
for (groupNbr in seq_along(sampleGroupsWithReplicates)) {
specificReplicateVals <- as.matrix(
methodData[, which(allReplicateGroups == sampleGroupsWithReplicates[groupNbr])])
class(specificReplicateVals) <- "numeric"
corVals <- stats::cor(
specificReplicateVals ,
use="pairwise.complete.obs",
method=corrType)
for (index in seq_len(ncol(specificReplicateVals) - 1)) {
corSums <- c(
corSums,
corVals[index, -(seq_len(index)), drop="FALSE"]
)
}
}
corSums
}
calculateSummarizedCorrelationVector <- function(
methodlist, allReplicateGroups, sampleGroupsWithReplicates, corrType) {
validCorrTypes <- c("pearson", "spearman")
if (!corrType %in% validCorrTypes) {
stop("Unknown correlation type: ",
corrType,
" valid are: ",
paste(validCorrTypes, collapse=", "))
}
corr_combination_count <- function(allReplicateGroups) {
replicate_counts <- table(allReplicateGroups)
sum(vapply(
replicate_counts,
function(count) { (count * (count-1)) / 2 },
0))
}
avgCorSum <- vapply(
methodlist,
calculateCorrSum,
rep(0, corr_combination_count(allReplicateGroups)),
allReplicateGroups=allReplicateGroups,
sampleGroupsWithReplicates=sampleGroupsWithReplicates,
corrType=corrType
)
avgCorSum
}
resNDE <- calculateSummarizedCorrelationVector(
methodlist = lista,
sampleGroupsWithReplicates = unique(simData$metadata$Groups),
allReplicateGroups = simData$metadata$Groups,
corrType = "spearman"
)
resNDE <- as.data.frame(resNDE)
is.data.frame(resNDE)
Biomics::plotBoxMulti(resNDE, varResumen = colnames(resNS), interact = F)
cbind(data[,seq(1,40, 2)] + rnorm(n = 20, mean = 0.3, sd = 5), data[,seq(2,40, 2)])
Met3 <- cbind(data[,seq(1,40, 2)] + rnorm(n = 20, mean = 0.3, sd = 5), data[,seq(2,40, 2)])
Met3 <- Met3[, colnames(data)]
lista <- list(
Log = data,
Met1 = data+0.5,
Met2 = (data*median(data, na.rm = T))/mean(data, na.rm = T),
Met3 = Met3
)
withinGroupCorrelations <- function(data, groupData, method = "spearman") {
data <- as.data.frame(data)
groupData <- as.data.frame(groupData)
allCorrelations <- lapply(unique(groupData$Groups), function(group) {
samplesByGroup <- groupData[groupData$Groups == group, "Samples"]
groupMatrix <- data[, samplesByGroup, drop = FALSE]
corMatrix <- stats::cor(groupMatrix, use = "pairwise.complete.obs", method = method)
corValues <- c()
for (i in seq_len(ncol(groupMatrix) - 1)) {
corValues <- c(corValues, corMatrix[i, -(seq_len(i))])
}
corValues
})
unlist(allCorrelations, use.names = FALSE)
}
resNS <- sapply(lista, withinGroupCorrelations, groupData = simData$metadata,
method = "spearman", simplify = T, USE.NAMES = T)
resNS <- as.data.frame(resNS)
is.data.frame(resNS)
Biomics::plotBoxMulti(resNS, varResumen = colnames(resNS), interact = F)
View(Met3)
#............................................................................####
# NormalyzerDE ####
calculateCorrSum <- function(methodData, allReplicateGroups,
sampleGroupsWithReplicates, corrType) {
corSums <- vector()
for (groupNbr in seq_along(sampleGroupsWithReplicates)) {
specificReplicateVals <- as.matrix(
methodData[, which(allReplicateGroups == sampleGroupsWithReplicates[groupNbr])])
class(specificReplicateVals) <- "numeric"
corVals <- stats::cor(
specificReplicateVals ,
use="pairwise.complete.obs",
method=corrType)
for (index in seq_len(ncol(specificReplicateVals) - 1)) {
corSums <- c(
corSums,
corVals[index, -(seq_len(index)), drop="FALSE"]
)
}
}
corSums
}
calculateSummarizedCorrelationVector <- function(
methodlist, allReplicateGroups, sampleGroupsWithReplicates, corrType) {
validCorrTypes <- c("pearson", "spearman")
if (!corrType %in% validCorrTypes) {
stop("Unknown correlation type: ",
corrType,
" valid are: ",
paste(validCorrTypes, collapse=", "))
}
corr_combination_count <- function(allReplicateGroups) {
replicate_counts <- table(allReplicateGroups)
sum(vapply(
replicate_counts,
function(count) { (count * (count-1)) / 2 },
0))
}
avgCorSum <- vapply(
methodlist,
calculateCorrSum,
rep(0, corr_combination_count(allReplicateGroups)),
allReplicateGroups=allReplicateGroups,
sampleGroupsWithReplicates=sampleGroupsWithReplicates,
corrType=corrType
)
avgCorSum
}
resNDE <- calculateSummarizedCorrelationVector(
methodlist = lista,
sampleGroupsWithReplicates = unique(simData$metadata$Groups),
allReplicateGroups = simData$metadata$Groups,
corrType = "spearman"
)
resNDE <- as.data.frame(resNDE)
is.data.frame(resNDE)
Biomics::plotBoxMulti(resNDE, varResumen = colnames(resNDE), interact = F)
summary(arsenal::comparedf(x = resNS, y = resNDE, by = "row.names"))
withinGroupCorrelations <- function(data, groupData, method = "spearman") {
data <- as.data.frame(data)
groupData <- as.data.frame(groupData)
allCorrelations <- lapply(unique(groupData$Groups), function(group) {
samplesByGroup <- groupData[groupData$Groups == group, "Samples"]
groupMatrix <- data[, samplesByGroup, drop = FALSE]
corMatrix <- stats::cor(groupMatrix, use = "complete.obs", method = method)
corValues <- c()
for (i in seq_len(ncol(groupMatrix) - 1)) {
corValues <- c(corValues, corMatrix[i, -(seq_len(i))])
}
corValues
})
unlist(allCorrelations, use.names = FALSE)
}
resNS <- sapply(lista, withinGroupCorrelations, groupData = simData$metadata,
method = "spearman", simplify = T, USE.NAMES = T)
resNS <- as.data.frame(resNS)
is.data.frame(resNS)
Biomics::plotBoxMulti(resNS, varResumen = colnames(resNS), interact = F)
summary(arsenal::comparedf(x = resNS, y = resNDE, by = "row.names"))
normScore::normScore()
resNS <- lapply(lista, withinGroupCorrelations, groupData = simData$metadata,
method = "spearman")
View(resNS)
allCorrelationVectors <- resNS
dfCor <- data.frame(sapply(allCorrelationVectors, "length<-",
max(lengths(allCorrelationVectors))))
View(dfCor)
View(dfCor)
resNS <- sapply(lista, withinGroupCorrelations, groupData = simData$metadata,
method = "spearman", simplify = T, USE.NAMES = T)
sapply(allCorrelationVectors, "length<-",
max(lengths(allCorrelationVectors)))
remove.packages("Biomics")
preview_site()
pkgdown::preview_site()
setwd("C:/Users/julia/Documents/GitHub/normScore")
pkgdown::preview_site()
knitr::opts_chunk$set(echo = TRUE)
pkgdown::build_home()
pkgdown::preview_site()
pkgdown::clean_site()
pkgdown::build_site()
pkgdown::clean_site()
pkgdown::build_site()
pkgdown::preview_site(path='articles/index.html')
pkgdown::preview_site(path='reference/index.html')
setwd("C:/Users/julia/Documents/GitHub/normScore")
knitr::opts_chunk$set(echo = TRUE)
pkgdown::build_article("normScore-method")
usethis::use_vignette("normScore-method")
pkgdown::build_article("normScore-method")
pkgdown::build_article("normScore-intro")
pkgdown::preview_site()
pkgdown::build_article("normScore-method")
pkgdown::preview_site()
